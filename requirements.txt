# ===========================================================================
# Bước 1: Cài torch CUDA trước (dùng requirements-torch.txt)
#   pip install -r requirements-torch.txt
#
# Bước 2: Cài các thư viện còn lại (dùng file này)
#   pip install -r requirements.txt --no-build-isolation
#
# Hoặc chạy: .\install.ps1
# ===========================================================================

# Core
setuptools<81
wheel>=0.42.0
transformers>=4.46.0,<5.0.0
accelerate>=0.34.0

# Quantization (4-bit / 8-bit)
bitsandbytes>=0.44.0,<0.47.0

# Tokenizer nhanh
tokenizers>=0.20.0,<0.22.0

# Dataset
datasets>=3.0.0,<4.0.0

# Training / Fine-tuning
peft>=0.13.0,<0.16.0
trl>=0.12.0,<0.17.0

# Logging & Experiment tracking
wandb>=0.18.0

# Utilities
sentencepiece>=0.2.0
protobuf>=4.25.0
scipy>=1.10.0,<1.16.0
matplotlib>=3.7.0

# Vietnamese ASR (Zipformer)
sherpa-onnx>=1.10.40
sounddevice>=0.4.7
numpy>=1.24.0,<2.3.0

# Whisper-live & Whisper dependencies
whisper-live>=0.7.0
openai-whisper>=20231117
